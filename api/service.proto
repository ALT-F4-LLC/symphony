syntax = "proto3";

option go_package = "github.com/erkrnt/symphony/api";

package api;

enum ServiceType {
  MANAGER = 0;
  BLOCK = 1;
}

message Service {
  string id = 1;
  string addr = 2;
  string type = 3;
}
